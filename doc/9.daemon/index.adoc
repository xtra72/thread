== User Thread vs Daemon Thread

* Java에서의 스레드는 일반 스레드(User Thread)와 데몬 스레드(Daemon Thread)로 나뉜다.
* 일반 스레드는 특별히 데몬 스레드로 설정하지 않은 스레드로서 일반적으로 생성해서 사용되는 스레드이다.
* JVM은 모든 일반 스레드가 종료될 때까지 프로그램을 실행한다.
* 데몬 스레드는 백그라운드에서 동작하는 스레드로, 일반 스레드가 종료될 때 자동으로 종료된다.
* 주로 메인 스레드나 다른 일반 스레드의 보조 역할을 수행하거나, 특정 작업을 주기적으로 처리하는 스레드 등에 사용된다.
* JVM은 모든 일반 스레드가 종료되면 데몬 스레드를 강제로 종료합니다.

{empty} +

=== Exam-01. 일반 스레드와 데몬 스레드를 생성하고, 일정 시간 후 메인 스레드를 종료한 후 각 스레드의 상태를 확인 하자

* 1초에 한번씩 메시지를 출력하는 스레드 두개를 생성한다.
* 하나는 setDaemon() 함수를 이용해 데몬 스레드로 설정한다.
* 두개의 스레드를 실행한다.
* 일반 스레드에 인터럽트를 발생시켜 종료 시킨다.
* 메인 스레드가 종료되도록 한 후 데몬 스레드의 상태를 확인한다.

[source,java]
----
include::../../src/main/java/com/nhnacademy/daemon/Exam01.java[]
---- 


=== Exam-02. 일반 스레드와 데몬 스레드를 생성한다. 단, 데몬 스레드는 일반 스레드 안에서 실행하도록 수정한 후 실행 종료 후 각 스레드의 상태를 확인 하자
* 1초에 한번씩 메시지를 출력하는 스레드 두개를 생성한다.
* 하나의 스레드는 실행된 후 내부에서 실행 되도록 구성하고, 이를 데몬 스레드로 한다.
* 일반 스레드에 인터럽트를 발생시켜 종료 시킨다.
* 메인 스레드가 종료되도록 한 후 데몬 스레드의 상태를 확인한다.
* 각 스레드는 내부에 반복 구간이 있으며, 스레드 종료시 이를 정상적으로 빠져 나오는지 확인한다.

[source,java]
----
include::../../src/main/java/com/nhnacademy/daemon/Exam02.java[]
----

{empty} + 

=== 정리
* 모든 스레드의 생성과 종료를 직접 관리하지 않을 수도 있다.
* 데몬 스레드의 우선 순위는 낮으나 조정 가능하다.